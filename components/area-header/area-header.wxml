<!--components/area-header/area-header.wxml-->
<view class="area-header">
  <view class="title">{{title}}</view>
  <view class="more" wx:if="{{isMore}}" bind:tap="onHaederTouch">{{more}}</view>
</view>